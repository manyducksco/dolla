### Conceptual outline

- Reactivity
  - Signals
  - tracking contexts (compose, effects, attributes and children)
- Components
  - context chain
  - lifecycle: `onMount`, `onCleanup` (and `onEffect`?)
  - Views
    - helpers (`forEach`, `showIf`, `hideIf` `createPortal`)
    - reactive props/attrs
    - class / style / data (specially handled attributes)
  - Stores
    - `addStore` / `getStore`
- Extras
  - Router
  - Translate
  - HTTP?
  - Virtual?
